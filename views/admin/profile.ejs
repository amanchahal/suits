<script type="text/javascript" src="/javascripts/jquery.validate.min.js"></script>
<script type="text/javascript" src="/javascripts/additional-methods.min.js"></script>
<script type="text/javascript" >
   jQuery(document).ready(function(){
        jQuery("#pass-form").validate({
            rules: {
                newpass: {
					required: true
				},
				repass: {
					required: true,
					equalTo: "#newpass"
				}
            },
            messages:{
                newpass: {
					required: "Please provide a password",
				},
				repass: {
					required: "Please provide a password",
					equalTo: "Please enter the same password as above"
				}
            }
        });
   });
</script>
<div class="col-md-9 col-sm-8 col-xs-12 "> 
    <div class="form-box">                                          
  
  
  <form method="post" action="/admin/profile" id="pass-form">
    <%
         if (typeof flash != 'undefined') {
            %>
            <div role="alert" class="alert alert-success alert-dismissible fade in">
               <button aria-label="Close" data-dismiss="alert" class="close" type="button">
                  <span aria-hidden="true">x</span>
               </button>
               <%= flash %>   
            </div>
            <%
            
         }
      %>
	  <%
         if (typeof errflash != 'undefined') {
            %>
            <div role="alert" class="alert alert-danger alert-dismissible fade in">
               <button aria-label="Close" data-dismiss="alert" class="close" type="button">
                  <span aria-hidden="true">x</span>
               </button>
               <%= errflash %>   
            </div>
            <%
            
         }
      %>
  <div class="user-name">User Name</div>
    
    <input type="text" class="form-control" name="username" value="<%= username %>" disabled=disabled>
 
 <div class="user-name">Old Password</div>
    
    <input type="password" class="form-control" name= "oldpass" required />
    
    <div class="user-name">New Password</div>
      <input type="password" class="form-control" name="newpass" id="newpass" required />
      
      <div class="user-name">Retype Pawword</div>
      <input type="password" class="form-control" name="repass" id="repass" required />
      
 <button type="submit" class="submit-btn">Submit</button>
 <button type="submit" class="cancel-btn">Cancel</button>
 
</form>
</div>
</div>

  </div>
  </div>
</div>